<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title default="ClawdEx">
      {assigns[:page_title]}
    </.live_title>
    <link phx-track-static rel="stylesheet" href={~p"/assets/css/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/js/app.js"}>
    </script>
    <style>
      /* Custom styles for ClawdEx */
      .btn-primary {
        @apply px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-medium transition-colors;
      }
      .btn-secondary {
        @apply px-4 py-2 bg-gray-700 hover:bg-gray-600 text-white rounded-lg font-medium transition-colors;
      }
      .sidebar-link {
        @apply flex items-center gap-3 px-4 py-3 rounded-lg text-gray-300 hover:bg-gray-800 hover:text-white transition-colors;
      }
      .sidebar-link.active {
        @apply bg-gray-800 text-white;
      }
      /* Hide scrollbar but keep functionality */
      .scrollbar-thin::-webkit-scrollbar {
        width: 6px;
      }
      .scrollbar-thin::-webkit-scrollbar-track {
        background: transparent;
      }
      .scrollbar-thin::-webkit-scrollbar-thumb {
        background: #4b5563;
        border-radius: 3px;
      }
    </style>
  </head>
  <body class="h-full bg-gray-900 text-gray-100">
    <div class="flex h-full">
      <!-- Sidebar -->
      <aside class="w-64 bg-gray-950 border-r border-gray-800 flex flex-col">
        <!-- Logo -->
        <div class="p-4 border-b border-gray-800">
          <a href="/" class="flex items-center gap-3">
            <span class="text-2xl">🤖</span>
            <span class="text-xl font-bold text-white">ClawdEx</span>
          </a>
        </div>

        <!-- Navigation -->
        <nav class="flex-1 p-4 space-y-1" id="sidebar-nav">
          <a href="/" class="sidebar-link" data-path="/">
            <span class="text-xl">📊</span>
            <span>Dashboard</span>
          </a>
          <a href="/chat" class="sidebar-link" data-path="/chat">
            <span class="text-xl">💬</span>
            <span>Chat</span>
          </a>
          <a href="/sessions" class="sidebar-link" data-path="/sessions">
            <span class="text-xl">📋</span>
            <span>Sessions</span>
          </a>
          <a href="/agents" class="sidebar-link" data-path="/agents">
            <span class="text-xl">🤖</span>
            <span>Agents</span>
          </a>
        </nav>
        <script>
          // Highlight active nav link
          (function() {
            const path = window.location.pathname;
            document.querySelectorAll('#sidebar-nav a').forEach(link => {
              const linkPath = link.dataset.path;
              if (linkPath === '/' ? path === '/' : path.startsWith(linkPath)) {
                link.classList.add('active');
              }
            });
          })();
        </script>

        <!-- Footer -->
        <div class="p-4 border-t border-gray-800">
          <div class="text-xs text-gray-500">
            ClawdEx v0.1.0
          </div>
        </div>
      </aside>

      <!-- Main Content -->
      <main class="flex-1 overflow-auto scrollbar-thin">
        <!-- Flash Messages -->
        <div class="fixed top-4 right-4 z-50 space-y-2" id="flash-container">
          <%= if Phoenix.Flash.get(@flash, :info) do %>
            <div class="bg-blue-600 text-white px-4 py-3 rounded-lg shadow-lg flex items-center gap-2" role="alert" phx-click={JS.push("lv:clear-flash", value: %{key: "info"}) |> JS.hide()}>
              <span>ℹ️</span>
              <span><%= Phoenix.Flash.get(@flash, :info) %></span>
              <button class="ml-auto">✕</button>
            </div>
          <% end %>
          <%= if Phoenix.Flash.get(@flash, :error) do %>
            <div class="bg-red-600 text-white px-4 py-3 rounded-lg shadow-lg flex items-center gap-2" role="alert" phx-click={JS.push("lv:clear-flash", value: %{key: "error"}) |> JS.hide()}>
              <span>⚠️</span>
              <span><%= Phoenix.Flash.get(@flash, :error) %></span>
              <button class="ml-auto">✕</button>
            </div>
          <% end %>
        </div>

        {@inner_content}
      </main>
    </div>
  </body>
</html>
