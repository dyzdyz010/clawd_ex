#!/bin/sh
# Release environment variables
# This file is evaluated by the release boot script.

# Load .env file if present alongside the release
ENV_FILE="${RELEASE_ROOT}/.env"
if [ -f "$ENV_FILE" ]; then
  set -a
  . "$ENV_FILE"
  set +a
fi
